#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

vector<int> v;
int n, m, c;

bool solve(int waitTime)
{
    for (int i = 1; i < n; i++){

    }
}

void BS(int left, int right)
{
    int mid = (right+left)/2;

}

int main()
{
    cin >> n >> m >> c;
    for (int i = 0; i < n; i++){
        int a; cin >> a;
        v.push_back(a);
    }

    sort(v.begin(), v.end());


}